<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.ivonzhang.views"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="10" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light.NoTitleBar">
        <activity
            android:name="cn.ivonzhang.views.ActSplashScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="MainActivity"></activity>
        <!-- 主页 -->
        <activity
            android:name="cn.displayboard.homeview.MainActivity"
            android:label="@string/app_name" >
        </activity>
        <!-- 登陆 -->
        <activity 
            android:name="cn.displayboard.login.UserLogin"></activity>
        <!-- 注册 -->
        <activity 
            android:name="cn.displayboard.register.RegisterNewUser"></activity>
        <activity 
            android:name="cn.displayboard.homeview.DetailsActivity"></activity>
        <activity 
            android:name="cn.displayboard.homeview.MenuDialog"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"></activity>
    </application>

</manifest>